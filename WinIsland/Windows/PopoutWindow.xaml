<Window x:Class="WinIsland.PopoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        xmlns:local="clr-namespace:WinIsland"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        Activated="Window_Activated"
        Deactivated="Window_Deactivated"
        SizeChanged="Window_SizeChanged"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Acrylic"
        ui:WindowHelper.ApplyBackground="True"
        ui:TitleBar.Height="0"
        StateChanged="Window_StateChanged"
        Title="PopoutWindow" Height="600" Width="800">
    <Grid Background="Transparent" x:Name="mainWindowGrid" WindowChrome.IsHitTestVisibleInChrome="True">
        <Grid.Resources>
            <Style x:Name="buttonStyle" TargetType="{x:Type Button}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border RenderTransformOrigin="0.5,0.5" x:Name="ButtonGrid" Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Border.RenderTransform>
                                    <ScaleTransform/>
                                </Border.RenderTransform>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Duration="0:0:0.1" Storyboard.TargetName="ButtonGrid" Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="0.85">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BounceEase EasingMode="EaseInOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Duration="0:0:0.1" Storyboard.TargetName="ButtonGrid" Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="0.85">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseInOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Duration="0:0:0.05" Storyboard.TargetName="ButtonGrid" Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" To="1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseInOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Duration="0:0:0.05" Storyboard.TargetName="ButtonGrid" Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" To="1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseInOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#33FFFFFF"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#22FFFFFF"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Foreground" Value="Gray"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="True">
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static ui:ThemeKeys.DefaultTextForegroundThemeBrushKey}}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0.5" Fill="{DynamicResource {x:Static ui:ThemeKeys.ApplicationPageBackgroundThemeBrushKey}}"/>
        <Border x:Name="windowBorder" BorderThickness="0">
            <Grid>
                <Image VerticalAlignment="Center" ClipToBounds="True" Margin="-30" x:Name="bg" Opacity="0.2" Stretch="UniformToFill">
                    <Image.Effect>
                        <BlurEffect Radius="40" RenderingBias="Performance"/>
                    </Image.Effect>
                </Image>
                <Label x:Name="windowTitle" FontSize="32" Margin="31,37,0,0" FontFamily="Segoe UI" Content="Window Popout Title - OwO"  VerticalContentAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Label.RenderTransform>
                        <TranslateTransform x:Name="windowTitleTransform"/>
                    </Label.RenderTransform>
                </Label>
                <TabControl ui:TabControlHelper.IsAddTabButtonVisible="False" ui:TabControlHelper.CloseButtonOverlayMode="Auto" x:Name="tabControl" SelectionChanged="TabControl_SelectionChanged" Margin="0,107,0,0" BorderThickness="0">
                    <TabControl.Resources>
                    </TabControl.Resources>
                    <!--Tabs-->
                    <TabItem x:Name="main" Header="Main" Background="#00000000" Foreground="{DynamicResource {x:Static ui:ThemeKeys.DefaultTextForegroundThemeBrushKey}}">
                        <ui:TabItemHelper.Icon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Home}"/>
                        </ui:TabItemHelper.Icon>
                    </TabItem>
                    <TabItem x:Name="weather" Header="Weather" Background="#00000000" Foreground="{DynamicResource {x:Static ui:ThemeKeys.DefaultTextForegroundThemeBrushKey}}">
                        <ui:TabItemHelper.Icon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Cloud}"/>
                        </ui:TabItemHelper.Icon>
                    </TabItem>
                    <TabItem x:Name="advanced" Header="Advanced" Background="#00000000" Foreground="{DynamicResource {x:Static ui:ThemeKeys.DefaultTextForegroundThemeBrushKey}}">
                        <ui:TabItemHelper.Icon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Settings}"/>
                        </ui:TabItemHelper.Icon>
                    </TabItem>
                </TabControl>
                <Frame x:Name="contentFrame" Margin="0,154,0,0" Background="Transparent" NavigationUIVisibility="Hidden" Navigating="contentFrame_Navigating" Navigated="contentFrame_Navigated">
                    <Frame.RenderTransform>
                        <TranslateTransform x:Name="frameAnimation"/>
                    </Frame.RenderTransform>
                </Frame>
                <Grid x:Name="titleBar" Height="32" VerticalAlignment="Top" Background="#00000000" MouseDown="titleBar_MouseDown">
                    <StackPanel FlowDirection="RightToLeft" Orientation="Horizontal">
                        <Button x:Name="closeButton" MouseLeave="closeButton_MouseLeave" MouseDown="closeButton_MouseDown" MouseEnter="closeButton_MouseEnter"  Margin="0,0,0,00" VerticalAlignment="Center" Width="42" Height="32" Content="&#xE8bb;" FontFamily="../Fonts/#Segoe Fluent Icons" Click="closeButton_Click"/>
                        
                        <Button x:Name="minimizeButton" Margin="0,0,0,00" VerticalAlignment="Center" Width="42" Height="32" Content="&#xe921;" FontFamily="../Fonts/#Segoe Fluent Icons" Click="minimizeButton_Click"/>
                    </StackPanel>
                </Grid>
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5">
                    <Label x:Name="versionWatermark" Content="WinIsland version 0" />
                    <Label Content="This is an alpha release bugs are expected. Report them by making a new github issue." />
                    <Label Content="Close button on the tabs are a bug." />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
